#include "main.h"
/**
*print_alphabet - print alphabet
*main - prin
*Return: empty from the void function
*/

void print_alphabet(void)

{
char alpha = 'a';
while (alpha <= 'z')
{
putchar(alpha);
alpha++;
}
putchar('\n');
}
