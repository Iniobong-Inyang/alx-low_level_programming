#include <stdio.h>
/**
*main - put am for betty body wotowoto
*Return: Always 0 (success)*
*/
int main(void)
{
char henry = 'a';
char ini = 'A';
for (henry = 'a' ; henry <= 'z'; henry++)
{
putchar(henry);
}
for (ini = 'A' ; ini <= 'Z'; ini++)
{
putchar(ini);
}
putchar('\n');
return (0);
}
